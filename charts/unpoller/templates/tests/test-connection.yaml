apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "unpoller.fullname" . }}-test-connection"
  labels:
    {{- include "unpoller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: "{{ .Values.tests.image.registry }}/{{ .Values.tests.image.repository }}:{{ .Values.tests.image.tag }}"
      imagePullPolicy: {{ .Values.tests.image.pullPolicy }}
      command: ['wget']
      args: ['{{ include "unpoller.fullname" . }}:{{ .Values.service.port }}{{ .Values.tests.path }}']
  restartPolicy: Never
