suite: unpoller PrometheusRule
templates:
  - templates/prometheusrule.yaml
release:
  name: rel
tests:
  - it: does not render when disabled
    set:
      metrics.prometheusRule.enabled: false
    asserts:
      - hasDocuments: { count: 0 }
  - it: omits groups entirely when enabled with no rules
    set:
      metrics.prometheusRule.enabled: true
    asserts:
      - hasDocuments: { count: 1 }
      - notExists: { path: spec.groups }
  - it: renders with one rule when enabled
    set:
      metrics.prometheusRule.enabled: true
      metrics.prometheusRule.rules:
        - alert: UnpollerTargetDown
          annotations:
            summary: unpoller metrics endpoint is down.
          expr: up{job=~".*unpoller.*"} == 0
          for: 5m
          labels:
            severity: warning
    asserts:
      - hasDocuments: { count: 1 }
      - equal: { path: apiVersion, value: monitoring.coreos.com/v1 }
      - equal: { path: kind, value: PrometheusRule }
      # release name deliberately does not contain the chart name, so fullname
      # (metadata.name) and name (the group) render differently and cannot be swapped
      - equal: { path: metadata.name, value: rel-unpoller }
      - equal:
          path: spec.groups[0].name
          value: unpoller
      - equal:
          path: spec.groups[0].rules[0].alert
          value: UnpollerTargetDown
      - equal:
          path: spec.groups[0].rules[0].expr
          value: up{job=~".*unpoller.*"} == 0
  - it: merges custom labels into metadata labels
    set:
      metrics.prometheusRule.enabled: true
      metrics.prometheusRule.labels:
        release: kube-prometheus-stack
      metrics.prometheusRule.rules:
        - alert: UnpollerTargetDown
          expr: up{job=~".*unpoller.*"} == 0
    asserts:
      - equal:
          path: metadata.labels.release
          value: kube-prometheus-stack
      - equal:
          path: metadata.labels["app.kubernetes.io/name"]
          value: unpoller
