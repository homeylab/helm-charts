suite: bookstack-file-exporter cronjob
templates:
  - templates/cronjob.yaml
release:
  name: bookstack-file-exporter
tests:
  - it: renders the CronJob by default (config.run_interval == 0)
    asserts:
      - hasDocuments: { count: 1 }
      - isKind: { of: CronJob }
  - it: does not render when config.run_interval > 0 (Deployment mode instead)
    set:
      config.run_interval: 60
    asserts:
      - hasDocuments: { count: 0 }
  - it: renders the container image as registry/repository:tag
    asserts:
      - equal:
          path: spec.jobTemplate.spec.template.spec.containers[0].image
          value: docker.io/homeylab/bookstack-file-exporter:3.0.0
  - it: overrides the image tag when image.tag is set
    set:
      image.tag: "3.0.1"
    asserts:
      - equal:
          path: spec.jobTemplate.spec.template.spec.containers[0].image
          value: docker.io/homeylab/bookstack-file-exporter:3.0.1
  - it: does not render timeZone by default (cron.timeZone unset)
    asserts:
      - notExists: { path: spec.timeZone }
  - it: renders timeZone when cron.timeZone is set (regression guard - previously read the wrong values path and never rendered)
    set:
      cron.timeZone: "America/New_York"
    asserts:
      - equal: { path: spec.timeZone, value: "America/New_York" }
  - it: mounts the chart-rendered ConfigMap by default
    asserts:
      - equal:
          path: spec.jobTemplate.spec.template.spec.volumes[0].configMap.name
          value: bookstack-file-exporter
  - it: mounts config.existingConfigMap when set, instead of the chart-rendered ConfigMap
    set:
      config.existingConfigMap: my-external-cm
    asserts:
      - equal:
          path: spec.jobTemplate.spec.template.spec.volumes[0].configMap.name
          value: my-external-cm
  - it: injects token env via secretKeyRef from the chart-managed Secret when auth.* is set
    set:
      auth.tokenId: my-id
      auth.tokenSecret: my-token-secret
    asserts:
      - equal:
          path: spec.jobTemplate.spec.template.spec.containers[0].env[0].name
          value: BOOKSTACK_TOKEN_ID
      - equal:
          path: spec.jobTemplate.spec.template.spec.containers[0].env[0].valueFrom.secretKeyRef.name
          value: bookstack-file-exporter
      - equal:
          path: spec.jobTemplate.spec.template.spec.containers[0].env[1].valueFrom.secretKeyRef.key
          value: BOOKSTACK_TOKEN_SECRET
  - it: points token secretKeyRef at existingSecret.name when set (chart Secret not used)
    set:
      existingSecret.name: my-secret
    asserts:
      - equal:
          path: spec.jobTemplate.spec.template.spec.containers[0].env[0].valueFrom.secretKeyRef.name
          value: my-secret
  - it: renders extraEnvFrom at envFrom[0]
    set:
      extraEnvFrom:
        - secretRef:
            name: eso-synced-secret
    asserts:
      - equal:
          path: spec.jobTemplate.spec.template.spec.containers[0].envFrom[0].secretRef.name
          value: eso-synced-secret
  - it: applies hardened securityContext defaults (UID/GID 33333)
    asserts:
      - equal: { path: "spec.jobTemplate.spec.template.spec.securityContext.runAsNonRoot", value: true }
      - equal: { path: "spec.jobTemplate.spec.template.spec.securityContext.runAsUser", value: 33333 }
      - equal: { path: "spec.jobTemplate.spec.template.spec.securityContext.runAsGroup", value: 33333 }
      - equal: { path: "spec.jobTemplate.spec.template.spec.securityContext.fsGroup", value: 33333 }
      - equal: { path: "spec.jobTemplate.spec.template.spec.containers[0].securityContext.allowPrivilegeEscalation", value: false }
      - contains:
          path: "spec.jobTemplate.spec.template.spec.containers[0].securityContext.capabilities.drop"
          content: ALL
  - it: renders imagePullSecrets on the job pod template
    # the Deployment path always had this; the CronJob path did not, and CronJob is
    # the default mode (config.run_interval == 0)
    set:
      imagePullSecrets:
        - name: myreg
        - name: backupreg
    asserts:
      - equal: { path: "spec.jobTemplate.spec.template.spec.imagePullSecrets[0].name", value: myreg }
      - equal: { path: "spec.jobTemplate.spec.template.spec.imagePullSecrets[1].name", value: backupreg }
  - it: omits imagePullSecrets when none are set
    asserts:
      - notExists: { path: spec.jobTemplate.spec.template.spec.imagePullSecrets }

  - it: leaves automountServiceAccountToken unset on the CronJob pod spec by default
    # the pod spec wins over the ServiceAccount, so writing true here would override
    # an external ServiceAccount that opted out - only the false case is rendered
    asserts:
      # anchor the render so the notExists below cannot pass on an absent document
      - exists:
          path: spec.jobTemplate.spec.template.spec.serviceAccountName
      - notExists:
          path: spec.jobTemplate.spec.template.spec.automountServiceAccountToken

  - it: honours serviceAccount.automount on the CronJob pod spec with an external ServiceAccount
    set:
      serviceAccount.create: false
      serviceAccount.automount: false
    asserts:
      - equal:
          path: spec.jobTemplate.spec.template.spec.serviceAccountName
          value: default
      - equal:
          path: spec.jobTemplate.spec.template.spec.automountServiceAccountToken
          value: false

  - it: does not force a token mount onto an external ServiceAccount from the CronJob
    # create false + automount true must stay silent so an external ServiceAccount
    # carrying automountServiceAccountToken false keeps its own opt-out. This chart
    # defaults create true, so the default case above does not cover this pairing -
    # and the CronJob is the default mode (config.run_interval == 0)
    set:
      serviceAccount.create: false
      serviceAccount.automount: true
    asserts:
      - equal:
          path: spec.jobTemplate.spec.template.spec.serviceAccountName
          value: default
      - notExists:
          path: spec.jobTemplate.spec.template.spec.automountServiceAccountToken

  - it: honours serviceAccount.automount on the CronJob pod spec with a chart-managed ServiceAccount
    # the gate is on automount alone, so a chart-managed ServiceAccount must opt the
    # pod out too - the SA object's own field does not reach an already-created pod template
    set:
      serviceAccount.create: true
      serviceAccount.automount: false
    asserts:
      - exists:
          path: spec.jobTemplate.spec.template.spec.serviceAccountName
      - equal:
          path: spec.jobTemplate.spec.template.spec.automountServiceAccountToken
          value: false

  # Optional pod-spec blocks. Static validation cannot see these: a pod spec
  # missing imagePullSecrets is schema-valid, so only a unit test catches a
  # `with` block that stopped rendering. Both directions are pinned - absent
  # when unset, and every entry present when set.
  - it: omits every optional pod-spec block when unset
    asserts:
      - notExists: { path: spec.jobTemplate.spec.template.spec.nodeSelector }
      - notExists: { path: spec.jobTemplate.spec.template.spec.affinity }
      - notExists: { path: spec.jobTemplate.spec.template.spec.tolerations }
      - notExists: { path: spec.jobTemplate.spec.template.metadata.annotations }

  # Two entries per list on purpose: one entry cannot catch a range that only
  # ever emits its first item, and two map keys catch a `toYaml` swapped for a
  # single-key interpolation. Lengths are pinned too, so a spurious third entry
  # fails rather than passing on the two that were checked.
  - it: renders every entry of every optional pod-spec block when set
    set:
      nodeSelector:
        kubernetes.io/arch: amd64
        topology.kubernetes.io/zone: us-east-1a
      tolerations:
        - key: first-taint
          operator: Exists
          effect: NoSchedule
        - key: second-taint
          operator: Exists
          effect: NoExecute
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/os
                    operator: In
                    values:
                      - linux
                      - windows
      podLabels:
        first-label: one
        second-label: two
      podAnnotations:
        first-annotation: one
        second-annotation: two
      volumes:
        - name: first-extra
          emptyDir: {}
        - name: second-extra
          emptyDir: {}
      volumeMounts:
        - name: first-extra
          mountPath: /first
        - name: second-extra
          mountPath: /second
    asserts:
      - equal:
          path: spec.jobTemplate.spec.template.spec.nodeSelector["kubernetes.io/arch"]
          value: amd64
      - equal:
          path: spec.jobTemplate.spec.template.spec.nodeSelector["topology.kubernetes.io/zone"]
          value: us-east-1a
      - lengthEqual: { path: "spec.jobTemplate.spec.template.spec.tolerations", count: 2 }
      - equal: { path: "spec.jobTemplate.spec.template.spec.tolerations[0].key", value: first-taint }
      - equal: { path: "spec.jobTemplate.spec.template.spec.tolerations[1].key", value: second-taint }
      - equal:
          path: "spec.jobTemplate.spec.template.spec.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution.nodeSelectorTerms[0].matchExpressions[0].values[1]"
          value: windows
      - equal:
          path: spec.jobTemplate.spec.template.metadata.labels["first-label"]
          value: one
      - equal:
          path: spec.jobTemplate.spec.template.metadata.labels["second-label"]
          value: two
      - equal:
          path: spec.jobTemplate.spec.template.metadata.annotations["first-annotation"]
          value: one
      - equal:
          path: spec.jobTemplate.spec.template.metadata.annotations["second-annotation"]
          value: two
      - contains:
          path: spec.jobTemplate.spec.template.spec.volumes
          content: { name: first-extra, emptyDir: {} }
      - contains:
          path: spec.jobTemplate.spec.template.spec.volumes
          content: { name: second-extra, emptyDir: {} }
      - contains:
          path: spec.jobTemplate.spec.template.spec.containers[0].volumeMounts
          content: { name: first-extra, mountPath: /first }
      - contains:
          path: spec.jobTemplate.spec.template.spec.containers[0].volumeMounts
          content: { name: second-extra, mountPath: /second }

  # extraEnv range: two entries + lengthEqual so a range that only ever emits
  # its first item, or drops one, fails.
  - it: renders every entry of extraEnv as upper-cased env vars
    set:
      extraEnv:
        first_var: value-one
        second_var: value-two
    asserts:
      - lengthEqual: { path: "spec.jobTemplate.spec.template.spec.containers[0].env", count: 2 }
      - contains:
          path: spec.jobTemplate.spec.template.spec.containers[0].env
          content: { name: FIRST_VAR, value: value-one }
      - contains:
          path: spec.jobTemplate.spec.template.spec.containers[0].env
          content: { name: SECOND_VAR, value: value-two }

  - it: mounts export-dump at persistence.mountPath when persistence.enabled
    set:
      persistence.enabled: true
      persistence.mountPath: /data/backups
    asserts:
      - equal: { path: "spec.jobTemplate.spec.template.spec.containers[0].volumeMounts[1].name", value: export-dump }
      - equal:
          path: "spec.jobTemplate.spec.template.spec.containers[0].volumeMounts[1].mountPath"
          value: /data/backups

  # resources is rendered at nindent 14 - one level deeper than most container
  # fields - so assert the nested limits/requests keys resolve, not just that
  # `resources` exists, to catch an indent regression that demotes it under
  # the wrong parent (or promotes it out of the container).
  - it: renders resources.limits and resources.requests when set
    set:
      resources:
        limits:
          cpu: 500m
          memory: 512Mi
        requests:
          cpu: 250m
          memory: 256Mi
    asserts:
      - equal: { path: "spec.jobTemplate.spec.template.spec.containers[0].resources.limits.cpu", value: 500m }
      - equal:
          path: "spec.jobTemplate.spec.template.spec.containers[0].resources.limits.memory"
          value: 512Mi
      - equal: { path: "spec.jobTemplate.spec.template.spec.containers[0].resources.requests.cpu", value: 250m }
      - equal:
          path: "spec.jobTemplate.spec.template.spec.containers[0].resources.requests.memory"
          value: 256Mi

  - it: mounts persistence.existingClaim as the export-dump PVC when set
    set:
      persistence.enabled: true
      persistence.existingClaim: my-existing-pvc
    asserts:
      - equal: { path: "spec.jobTemplate.spec.template.spec.volumes[1].name", value: export-dump }
      - equal:
          path: "spec.jobTemplate.spec.template.spec.volumes[1].persistentVolumeClaim.claimName"
          value: my-existing-pvc

  - it: mounts the chart-managed PVC as the export-dump volume when persistence.existingClaim is unset
    set:
      persistence.enabled: true
    asserts:
      - equal: { path: "spec.jobTemplate.spec.template.spec.volumes[1].name", value: export-dump }
      - equal:
          path: "spec.jobTemplate.spec.template.spec.volumes[1].persistentVolumeClaim.claimName"
          value: bookstack-file-exporter

  # cronjob.yaml has no ports/probe logic at all - the "Deployment mode only"
  # contract documented in values.yaml is currently enforced only by that
  # omission. This pins it so a future change that wires health_port into the
  # CronJob path does not slip through unnoticed.
  - it: renders no ports or probes in CronJob mode even when config.health_port is set (probes are Deployment-mode only)
    set:
      config.health_port: "8080"
    asserts:
      - notExists: { path: "spec.jobTemplate.spec.template.spec.containers[0].ports" }
      - notExists: { path: "spec.jobTemplate.spec.template.spec.containers[0].livenessProbe" }
      - notExists: { path: "spec.jobTemplate.spec.template.spec.containers[0].readinessProbe" }
