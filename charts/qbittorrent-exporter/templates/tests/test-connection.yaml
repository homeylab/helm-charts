{{- if .Values.tests.enabled }}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qbittorrent-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "qbittorrent-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: "{{ .Values.tests.image.registry }}/{{ .Values.tests.image.repository }}:{{ .Values.tests.image.tag }}"
      imagePullPolicy: {{ .Values.tests.image.pullPolicy }}
      command: ['wget']
      args: ['{{ include "qbittorrent-exporter.fullname" . }}:{{ .Values.service.port }}{{ .Values.tests.path }}']
  restartPolicy: Never
{{- end }}
