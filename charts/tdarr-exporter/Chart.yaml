apiVersion: v2
name: tdarr-exporter
description: A Helm chart to export Prometheus metrics for Tdarr.
type: application

home: https://github.com/homeylab/tdarr-exporter
sources:
  - https://github.com/homeylab/tdarr-exporter
  - https://github.com/homeylab/helm-charts
keywords:
  - tdarr
  - transcode
  - prometheus
  - exporter
  - metrics
maintainers:
  - name: homeylab
    url: https://github.com/homeylab

# This is the chart version. SemVer: https://semver.org/
version: 2.2.0

# Application version (upstream image tag). Not SemVer-bound.
# renovate: datasource=docker depName=homeylab/tdarr-exporter
appVersion: "3.0.0"

# Minimum supported Kubernetes version. The -0 suffix matches managed distros'
# prerelease-style versions (e.g. v1.28.3-gke.x).
kubeVersion: ">=1.19.0-0"

annotations:
  artifacthub.io/license: MIT
  artifacthub.io/links: |
    - name: Chart Source
      url: https://github.com/homeylab/helm-charts/tree/main/charts/tdarr-exporter
  artifacthub.io/changes: |
    - kind: security
      description: "serviceAccount.automount false is now honoured on the pod spec, not only on the chart-managed ServiceAccount. With serviceAccount.create false the setting was silently dropped, so a chart pointed at an external ServiceAccount mounted an API token regardless. Nothing is written to the pod spec when automount is true, so an external ServiceAccount that opts out of token mounting keeps its own setting. Default rendering is unchanged"
    - kind: fixed
      description: "the PrometheusRule is now gated on metrics.enabled as well as metrics.prometheusRule.enabled. It previously rendered even with the chart's Prometheus wiring - podAnnotations and ServiceMonitor - switched off. NOTE: if you run with metrics.enabled false and metrics.prometheusRule.enabled true, the PrometheusRule is REMOVED on upgrade - see the upgrade matrix in the README"
    - kind: changed
      description: "the serviceAccount value keys now carry helm-docs `# --` markers, so they appear in the README value table; no behavioural change"
    - kind: added
      description: "unit test coverage pinning both the default and the mutated value of serviceAccount.automount on the pod spec, and that the PrometheusRule stays unrendered with metrics disabled"
