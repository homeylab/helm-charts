~~Thanks for using homeylab charts!~~

Get the {{ .Chart.Name }} instances by running this command:
    - kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "tdarr-exporter.name" . }},app.kubernetes.io/instance={{ .Release.Name }}"
{{- if .Values.ingress.enabled }}

Exposed via Ingress:
{{- range $host := .Values.ingress.hosts }}
{{- range .paths }}
    - http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
{{- end }}
{{- end }}
{{- else if and .Values.httproute.enabled .Values.httproute.hostnames }}

Exposed via Gateway API HTTPRoute:
{{- range .Values.httproute.hostnames }}
    - http://{{ . }}/metrics
{{- end }}
{{- end }}
{{- if .Values.httproute.enabled }}

NOTE: HTTPRoute is enabled. This chart does not create a Gateway.
Ensure the Gateway API CRDs are installed and the Gateway referenced in
httproute.parentRefs exists and allows routes from namespace "{{ .Release.Namespace }}"
(via the listener's allowedRoutes or a ReferenceGrant).
{{- end }}

A sample Grafana dashboard is available from upstream:
    - https://grafana.com/grafana/dashboards/20388-tdarr
