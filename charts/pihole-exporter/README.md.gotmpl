<!-- This README is generated by helm-docs from README.md.gotmpl. Do NOT edit README.md directly; edit the .gotmpl and re-run helm-docs. -->
{{ template "chart.header" . }}
{{ template "chart.badgesSection" . }}![Kubernetes: {{ .KubeVersion }}](https://img.shields.io/badge/Kubernetes-{{ .KubeVersion | replace "-" "--" | replace "_" "__" | replace ">" "%3E" | replace "<" "%3C" | replace "=" "%3D" | replace " " "%20" }}-informational?style=flat-square)

{{ template "chart.description" . }}

Based on the [`ekofr/pihole-exporter`](https://github.com/eko/pihole-exporter) image.

## Table of Contents <!-- omit in toc -->
- [Add Chart Repo](#add-chart-repo)
- [Prerequisites](#prerequisites)
- [Install](#install)
- [Upgrade](#upgrade)
- [Configuration Options](#configuration-options)
- [Grafana Dashboards](#grafana-dashboards)
- [References](#references)

## Add Chart Repo
```bash
helm repo add homeylab https://homeylab.github.io/helm-charts/
# update the chart, this can also be run to pull new versions of the chart for upgrades
helm repo update homeylab
```

## Prerequisites
Requires a **Pi-hole v6+** server (exporter v1.x). Still on Pi-hole v5? Pin the chart to `0.0.2` (`--version 0.0.2`).

The exporter reads only `PIHOLE_PASSWORD`; the chart maps both `settings.auth.password` and the deprecated `settings.auth.token` to it (accepts a Pi-hole app password or the legacy WEBPASSWORD API token). When using `settings.auth.existingSecret`, the referenced secret must provide the `PIHOLE_PASSWORD` key.

## Install
```bash
helm install pihole-exporter homeylab/pihole-exporter -n pihole-exporter --create-namespace

# with own values file - recommended
helm install -f my-values.yaml pihole-exporter homeylab/pihole-exporter -n pihole-exporter --create-namespace
```

### OCI Registry Support
```bash
helm install pihole-exporter -n pihole-exporter oci://registry-1.docker.io/homeylabcharts/pihole-exporter --version X.Y.Z --create-namespace

# with own values file - recommended
helm install -f my-values.yaml pihole-exporter -n pihole-exporter oci://registry-1.docker.io/homeylabcharts/pihole-exporter --version X.Y.Z --create-namespace
```

### Install Example
Only some options are shown, view `values.yaml` for an exhaustive list.

<details closed>
<summary>custom-values.yaml</summary>
<br>

```yaml
# custom-values.yaml
settings:
  config:
    hostname: pi-hole.example.org
  auth:
    password: "fakepassword"

metrics:
  enabled: true
  serviceMonitor:
    enabled: true
  ## Optional alerting rules (requires the Prometheus Operator CRD)
  # prometheusRule:
  #   enabled: true
  #   ## labels your Prometheus ruleSelector matches on
  #   labels:
  #     release: kube-prometheus-stack
  #   rules:
  #     - alert: PiholeExporterTargetDown
  #       annotations:
  #         summary: pihole-exporter metrics endpoint is down.
  #       expr: up{job=~".*pihole-exporter.*"} == 0
  #       for: 5m
  #       labels:
  #         severity: warning

## Alternative to ingress - Gateway API HTTPRoute (chart does NOT create the Gateway)
# httproute:
#   enabled: true
#   parentRefs:
#     - name: my-gateway
#       namespace: gateway-system
#   hostnames:
#     - pihole-exporter.example.org
```
</details>
<br>

## Upgrade
```bash
helm upgrade pihole-exporter homeylab/pihole-exporter -n pihole-exporter

# with own values file - recommended
helm upgrade -f my-values.yaml pihole-exporter homeylab/pihole-exporter -n pihole-exporter
```

### OCI Registry Support
```bash
helm upgrade pihole-exporter -n pihole-exporter oci://registry-1.docker.io/homeylabcharts/pihole-exporter --version X.Y.Z

# with own values file - recommended
helm upgrade -f my-values.yaml pihole-exporter -n pihole-exporter oci://registry-1.docker.io/homeylabcharts/pihole-exporter --version X.Y.Z
```

### Breaking Changes

| Start Chart Version | Target Chart Version | Upgrade Steps |
| ------------------- | -------------------- | ------------- |
| `1.X.X` | `2.0.0` | The `helm test` image values moved from `testConnImage.*` to `tests.image.*`. **Read the [upgrade notes](https://github.com/homeylab/helm-charts/blob/main/charts/pihole-exporter/docs/upgrade.md#from-1xx-to-200) before upgrading.** |

_The [full upgrade notes](https://github.com/homeylab/helm-charts/blob/main/charts/pihole-exporter/docs/upgrade.md) cover every version, including upgrades gated on opt-in settings._


## Configuration Options
{{ template "chart.valuesTable" . }}

## Grafana Dashboards
Official dashboard from upstream [provider](https://grafana.com/grafana/dashboards/10176-pi-hole-exporter/).

## References
* https://github.com/eko/pihole-exporter
</content>
