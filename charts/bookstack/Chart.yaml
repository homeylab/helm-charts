apiVersion: v2
name: bookstack
description: bookstack documentation platform

# A chart can be either an 'application' or a 'library' chart.
#
# Application charts are a collection of templates that can be packaged into versioned archives
# to be deployed.
#
# Library charts provide useful utilities or functions for the chart developer. They're included as
# a dependency of application charts to inject those utilities and functions into the rendering
# pipeline. Library charts do not define any templates and therefore cannot be deployed.
type: application

icon: https://raw.githubusercontent.com/BookStackApp/BookStack/development/public/icon.png
home: https://www.bookstackapp.com/
sources:
  - https://github.com/BookStackApp/BookStack
  - https://github.com/linuxserver/docker-bookstack
  - https://github.com/homeylab/helm-charts
keywords:
  - bookstack
  - wiki
  - documentation
  - knowledge-base
maintainers:
  - name: homeylab
    url: https://github.com/homeylab

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 5.3.2

# This is the version number of the application being deployed. This version number should be
# incremented each time you make changes to the application. Versions are not expected to
# follow Semantic Versioning. They should reflect the version the application is using.
# It is recommended to use it with quotes.
# renovate: datasource=docker depName=linuxserver/bookstack versioning=regex:^version-v(?<major>\d+)\.(?<minor>\d+)(\.(?<patch>\d+))?$
appVersion: "version-v26.05.2"

# Minimum supported Kubernetes version. The -0 suffix matches managed distros'
# prerelease-style versions (e.g. v1.28.3-gke.x).
# 1.21, not the fleet's 1.19: this chart's own manifests are 1.19-clean, but the
# bookstack-file-exporter subchart renders a batch/v1 CronJob (GA in 1.21) and
# Helm does not enforce a subchart's own kubeVersion — so the floor has to cover
# every path this chart can enable, not just the default one.
kubeVersion: ">=1.21.0-0"

dependencies:
- name: mariadb
  version: "0.16.5"
  condition: mariadb.enabled
  repository: "oci://registry-1.docker.io/cloudpirates"
- name: bookstack-file-exporter
  version: "1.0.3"
  condition: bookstack-file-exporter.enabled
  repository: "oci://registry-1.docker.io/homeylabcharts"

annotations:
  artifacthub.io/license: MIT
  artifacthub.io/links: |
    - name: Chart Source
      url: https://github.com/homeylab/helm-charts/tree/main/charts/bookstack
  artifacthub.io/changes: |
    - kind: changed
      description: "the optional bookstack-file-exporter subchart is re-pinned from 1.0.2 to 1.0.3. Only relevant with bookstack-file-exporter.enabled true. No values keys changed between the two, so no configuration to migrate"
    - kind: fixed
      description: "the embedded exporter no longer provisions an orphan PersistentVolumeClaim when bookstack-file-exporter.persistence.existingClaim is set. NOTE: upgrading in that state DELETES the redundant PVC, and its PersistentVolume under the default Delete reclaim policy - see the Upgrade Matrix in the README before upgrading"
    - kind: fixed
      description: "the embedded exporter now applies imagePullSecrets to its CronJob pod template. Only its Deployment path honoured them, and the CronJob is that subchart's default mode (config.run_interval == 0), so private-registry users hit ImagePullBackOff"
